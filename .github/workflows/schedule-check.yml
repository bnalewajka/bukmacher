# Temporary: proves scheduled runs fire once the workflow's last commit has a GitHub-linked
# author. Delete after the first scheduled run.
name: schedule-check

on:
  schedule:
    - cron: "*/5 * * * *"

permissions: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scheduled run at $(date -u)"
